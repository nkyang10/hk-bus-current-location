{
  "name": "hk-bus-tracker",
  "version": "2.0.0",
  "description": "Hong Kong Bus Tracker - Real-time KMB/LWB bus arrival tracking",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "http-server": "^14.1.1"
  }
}
